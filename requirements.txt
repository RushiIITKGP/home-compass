# Phase 1 dependencies (db layer + ingest-worker).
# Later phases add their own requirements.txt inside api/, mcp-*/ etc.
# as those services get their own Docker build contexts.

sqlalchemy>=2.0.30
psycopg[binary]>=3.1.18
pgvector>=0.3.6
pandas>=2.2.2
python-dotenv>=1.0.1

# compliance/ — rules PDF generation + ingestion (RAG guardrail)
reportlab>=4.0
pdfplumber>=0.11
